# Compose project for the Synology (DSM Container Manager → Projects).
#
# Dev is native — this file is production-only. The web image is built automatically
# via GitHub Actions and published to GHCR. Watchtower automatically pulls updates
# and restarts web. A Caddy proxy sits in front so deploys don't 502 during the
# restart, and a cloudflared sidecar exposes the app at playquartets.com via a
# Cloudflare Tunnel (tunnel → caddy → web). Secrets live in a sibling .env on the
# NAS (gitignored) — see .env.example and docs/DEPLOY.md.
services:
  db:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: quartets
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD in .env}
      POSTGRES_DB: quartets_production
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U quartets -d quartets_production"]
      interval: 10s
      timeout: 5s
      retries: 5

  web:
    # Automatically built via GitHub Actions and published to GHCR.
    image: ghcr.io/johnhutch/quartets:latest
    restart: unless-stopped
    # Watchtower only auto-updates containers carrying this label (it runs with
    # WATCHTOWER_LABEL_ENABLE), so a new image cycles ONLY web — never the proxy,
    # db, or tunnel. That's what keeps the Caddy buffer up across a deploy.
    labels:
      - "com.centurylinklabs.watchtower.enable=true"
    depends_on:
      db:
        condition: service_healthy
    environment:
      # Single Postgres for app + Solid cache/queue/cable (see config/database.yml).
      DATABASE_URL: postgres://quartets:${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD in .env}@db:5432/quartets_production
      RAILS_MASTER_KEY: ${RAILS_MASTER_KEY:?set RAILS_MASTER_KEY in .env}
      # Superuser seed — db:prepare runs it on the first boot (db/seeds.rb).
      ADMIN_EMAIL: ${ADMIN_EMAIL:?set ADMIN_EMAIL in .env}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:?set ADMIN_PASSWORD in .env}
      # Run jobs inside Puma — no separate worker service.
      SOLID_QUEUE_IN_PUMA: "true"
      WEB_CONCURRENCY: "2"
      RAILS_MAX_THREADS: "3"
      DB_POOL: "12" # 3 for Puma + 5 for Solid Queue + overhead
      # Public host — mailer link host (share URLs follow the request host).
      APP_HOST: ${APP_HOST:-playquartets.com}
      # Outbound mail. Resend HTTP API (port 443) is preferred — set RESEND_API_KEY.
      # SMTP_* are a fallback, only used if RESEND_API_KEY is blank. Blank = mail off.
      MAILER_SENDER: ${MAILER_SENDER:-no-reply@playquartets.com}
      RESEND_API_KEY: ${RESEND_API_KEY:-}
      SMTP_ADDRESS: ${SMTP_ADDRESS:-}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_USERNAME: ${SMTP_USERNAME:-}
      SMTP_PASSWORD: ${SMTP_PASSWORD:-}
      SMTP_AUTHENTICATION: ${SMTP_AUTHENTICATION:-plain}
      # Error monitoring (Sentry). Blank = off; the app runs fine without it.
      # GIT_SHA is baked into the image at build time, so it isn't set here.
      SENTRY_DSN: ${SENTRY_DSN:-}
    # Puma directly on port 3000 (Caddy fronts it — no Thruster). Matches the
    # image's default CMD now; kept explicit as documentation.
    command: ["./bin/rails", "server"]
    # The Cloudflare Tunnel reaches it at web:3000 over the compose network.
    # The host-port mapping is just for direct LAN access / debugging.
    ports:
      - "${WEB_PORT:-3000}:3000"

  # Front proxy (see Caddyfile). cloudflared targets THIS, not web directly, so it
  # can hold/retry requests while Watchtower recreates web on a deploy — no 502s
  # during the ~10-15s restart. Caddy is pinned and never auto-updated, so it stays
  # up across deploys.
  caddy:
    image: caddy:2
    restart: unless-stopped
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
    depends_on:
      - web

  # Outbound-only tunnel: exposes the app at playquartets.com with no port
  # forwarding and no exposed home IP. Create the tunnel in Cloudflare Zero Trust,
  # set its public hostname → http://caddy:80, and drop the token in .env.
  cloudflared:
    image: cloudflare/cloudflared:latest
    restart: unless-stopped
    command: tunnel --no-autoupdate run
    environment:
      TUNNEL_TOKEN: ${TUNNEL_TOKEN:?set TUNNEL_TOKEN in .env}
    depends_on:
      - caddy

  # Automatically updates containers when a new image is pushed to GHCR
  watchtower:
    image: containrrr/watchtower
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    # Check for updates every 5 minutes (300 seconds)
    command: --interval 300
    environment:
      # Only watch containers labelled watchtower.enable=true — i.e. just web.
      WATCHTOWER_LABEL_ENABLE: "true"
      # If your GHCR repo is private, Watchtower needs authentication to pull.
      # You can supply a GitHub Personal Access Token (PAT) via .env
      REPO_USER: ${GITHUB_ACTOR:-}
      REPO_PASS: ${GITHUB_TOKEN:-}

volumes:
  pgdata:
  caddy_data:
